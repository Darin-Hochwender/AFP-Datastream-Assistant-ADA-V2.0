<Window x:Class="AFPEngineer_Explorer.TripletJsonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Triplet JSON Data" Height="500" Width="600" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OpenUrlBtn" Content="Open URL" Padding="10,5" Margin="0,0,10,0" Click="OpenUrl_Click" Background="#007ACC" Foreground="White" FontWeight="Bold" />
            <Button Content="Close" Padding="10,5" Click="Close_Click" />
        </StackPanel>

        <TextBlock x:Name="HeaderTxt" Grid.Row="1" FontSize="18" FontWeight="Bold" Margin="0,10,0,10" />

        <TextBox x:Name="JsonContentTxt" Grid.Row="2" FontFamily="Consolas" AcceptsReturn="True" IsReadOnly="True" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" TextWrapping="NoWrap" />
    </Grid>
</Window>
